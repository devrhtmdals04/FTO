{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // This package is bundled by Vite, so we don't need to emit compiled files from tsc.
    "noEmit": true
  },
  "include": ["src", "demo"]
}
